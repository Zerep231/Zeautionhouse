name: ZeAuctionHouse
version: '${project.version}'
main: dev.zerep.zeah.ZeAuctionHouse
api-version: '1.21'
description: Stable, secure, scalable AuctionHouse for Paper 1.21.1+
authors: [Zerep231]
website: https://github.com/Zerep231/Zeautionhouse

softdepend:
  - Vault

commands:
  ah:
    description: Main AuctionHouse command
    usage: /ah [browse|sell|claim|shop|mylistings|reload]
    aliases: [auctionhouse, auction]

permissions:
  zeah.use:
    description: Use the auction house
    default: true
  zeah.sell:
    description: Sell items
    default: true
  zeah.claim:
    description: Claim delivered items
    default: true
  zeah.shop:
    description: Use the builder shop
    default: true
  zeah.admin:
    description: Admin commands (reload, forceexpire, etc.)
    default: op
  zeah.bypass.limit:
    description: Bypass max listing limit
    default: op
  zeah.bypass.blacklist:
    description: Bypass item blacklist
    default: op
