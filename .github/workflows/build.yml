name: Build and Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build and Test with Maven
        run: mvn clean package -q

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: ZeAuctionHouse-${{ github.sha }}
          path: target/ZeAuctionHouse-*.jar
          retention-days: 30

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    permissions:
      contents: write
    steps:
      - name: Download JAR artifact
        uses: actions/download-artifact@v4
        with:
          name: ZeAuctionHouse-${{ github.sha }}
          path: ./plugin-jar

      - name: Extract version from JAR filename
        id: get_version
        run: |
          JAR_FILE=$(ls ./plugin-jar/ZeAuctionHouse-*.jar | head -n 1)
          VERSION=$(basename $JAR_FILE | sed -E 's/ZeAuctionHouse-(.*)\.jar/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: ZeAuctionHouse v${{ steps.get_version.outputs.version }}
          files: ./plugin-jar/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
